import numpy as np
import tkinter as tk
import random
import math

class Figure:
    def __init__(self, color, is_alive, x_cords):
        self.color = color
        self.is_alive = is_alive

        list_of_y_coords = ['A', 'B', 'C', 'D', 'E', 'F', 'G', 'H']
        self.x_cords = x_cords





